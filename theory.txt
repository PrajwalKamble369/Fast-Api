What is API?
API is connector which connect backend and frontend.
example: customer <-> waiter <-> kitchen
waiter is api in this case

FastAPI:
It is modern high performance web framework for building APIs with Python.

It is built on starlette and pydantic which are Python Liabraries.

api recive http request from client and perform some process on this request, and in return genrate respose in http in this starlette do this

pydantic is used to check if the data coming into your API is correct and in the right format.

Philosopy of FastAPI:
1) Old freameworks are slow, in this api run fast
2) Need to write to much code, in this fast to code

HTTP Methods
Softwares Types:
    1) Static - Calander, Clock (user interaction is less)
    2) Dynamic - interaction with user two way communacation (fb)

Types of interaction:
We can only do 4 times of interaction with dyanamic softwares and for that opearation new term is coined which is "crud"
    1) C = Create (POST)
    2) R = Retrive (GET)
    3) U = Update (PUT)
    4) D = Delete (DELETE)

Website are also a type of software but in case of website software is installed on differnt machine(server) and used on other machine(client)
This is like a two way communication between client and server and this entire communication is done through a protocol known as HTTP.

In website also we have static(blog,goverment websites) and dyanamic(instagram,X) 

HTTP Methods Types:
    1) POST
    2) GET
    3) PUT
    4) DELETE

Path Parameter:
Path parameters are dynamic segment of a URL used to identify a specific resource.
example : in our project localhost8000/view -----> return all patients
but localhost8000/view/8  ----> return specific patient and this /8 is dyanamic this is path paramete.

Path Function in FastAPI:
The Path() Function in FastAPI is used to provide metadata, validation rules and documentation hints for path parameters in your API endpoints.
Title
Description
Exampple
ge, gt, le, lt
Min_length
Max_length
regex