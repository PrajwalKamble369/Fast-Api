What is API?
API is connector which connect backend and frontend.
example: customer <-> waiter <-> kitchen
waiter is api in this case

FastAPI:
It is modern high performance web framework for building APIs with Python.

It is built on starlette and pydantic which are Python Liabraries.

api recive http request from client and perform some process on this request, and in return genrate respose in http in this starlette do this

pydantic is used to check if the data coming into your API is correct and in the right format.

Philosopy of FastAPI:
1) Old freameworks are slow, in this api run fast
2) Need to write to much code, in this fast to code

HTTP Methods
Softwares Types:
    1) Static - Calander, Clock (user interaction is less)
    2) Dynamic - interaction with user two way communacation (fb)

Types of interaction:
We can only do 4 times of interaction with dyanamic softwares and for that opearation new term is coined which is "crud"
    1) C = Create (POST)
    2) R = Retrive (GET)
    3) U = Update (PUT)
    4) D = Delete (DELETE)

Website are also a type of software but in case of website software is installed on differnt machine(server) and used on other machine(client)
This is like a two way communication between client and server and this entire communication is done through a protocol known as HTTP.

In website also we have static(blog,goverment websites) and dyanamic(instagram,X) 

HTTP Methods Types:
    1) POST
    2) GET
    3) PUT
    4) DELETE

Path Parameter:
Path parameters are dynamic segment of a URL used to identify a specific resource.
example : in our project localhost8000/view -----> return all patients
but localhost8000/view/8  ----> return specific patient and this /8 is dyanamic this is path paramete.

Path Function in FastAPI:
The Path() Function in FastAPI is used to provide metadata, validation rules and documentation hints for path parameters in your API endpoints.
Title
Description
Exampple
ge, gt, le, lt
Min_length
Max_length
regex

HTTP Status Code:
They are 3 digits numbers return by a web server (like FastAPI) to indicate
result of a client request (like from a browerser or API consumer)

            
____________                           _____________
|          |         request           |           |
| Client   |  --------------------->   |  Server   |
|__________|  <---------------------   |___________|
                    respose

They help the client(browser,frontend,mobile app etc) understand:
 1) whether the request was successful
 2) whether something went wrong
 3) and what kind of issue occure (if any)


 ___________________________________________________________________________________________
 | 2xx   ---> | Success        ---> | The request was successfully received and processed  |
 | 3xx   ---> | Redirection    ---> | Further action needs to be taken (e.g. redirect)     |
 | 4xx   ---> | Client Error   ---> | Something is wrong with the request from the client  |
 | 5xx   ---> | Server Error   ---> | Something went wrong on the server side              |
 |____________|_____________________|______________________________________________________|


__________________________________________________________________________________________
| 200 OK          |   Standard Success             |   A GET or POST succeeded            |
| 201 Created     |   Resource Created             |  After a POST that creates something |
| 204 No Content  |   Success but no data returned |  After a DELETE request              |
|_________________|________________________________|______________________________________|

HTTPException:
   HTTPException is special built in exception is FastAPI used to return custom HTTP error responses when something goes wrong.
   Insted of returning normal JSON or crashing the server, you can gracefully raise an error with:
    * a proper HTTP status code (like 404,400,403,etc)
    * a custom error message
    * (optinal) extra headers


Query Parameter:
    Query parameters are optinal key-valye pairs appended to the end of a URL,.
    used to pass additional data to the server in an HTTP request. They are typically
    employed for opearations like filtering, sorting, searching and pagination without
    altering the endpoint path itself

    /patients?city=Delhi&sort_by=age

        * The ? marks the start if query parameters.
        * Each parameter is a key-value pair: key=pair
        * Multiple parameters are seprated by &
    In this case
        * city=Delhi is query parameter for filtering
        * sort_by=age is query parameter for sorting

    Query() is a utility function provided by FastAPI to declare,validate,
    and document query parameters in your API endpoints.
    